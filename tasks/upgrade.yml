- name: Do Distro Upgrade
  become: true
  ansible.builtin.apt:
    upgrade: dist
    update_cache: yes

- name: Remove dependencies that are no longer required.
  ansible.builtin.apt:
    autoremove: yes