## check if bin foler exsit
if [-d $HOME/.bin] 
## add folder to var PATH
then PATH=$PATH:$HOME/.bin
## if folder not there
else mkdir $HOME/.bin .... PATH=$PATH:$HOME/.bin
fi

## check if file exsit
if [-f $HOME/.bin/filename]
## do nothing
then
## add file to folder
else copy file to $HOME/.bin
fi

Script in /usr/bin ## for all user 
or /$HOME/.bin ## for you only...

## send email with bash
install sendEmail libio-socket-ssl-perl libnet-ssleay-perl

sendEmail -f "from" -t "to" -u "subjet" -m "messagebody" -s "stmp" -xu "user" -xp "pass"


